import {addOrderArr} from ".../src/view-controller/functions.js"

describe('addOrderArr', () => {
    it('debería ser una función', () => {
      expect(typeof addOrderArr).toBe('function');
    });
});